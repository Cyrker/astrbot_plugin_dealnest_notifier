{
  "enabled": {
    "description": "是否启用 DealNest QQ 群通知拉取",
    "type": "bool",
    "default": true
  },
  "dealnest_base_url": {
    "description": "DealNest 站点地址",
    "type": "string",
    "hint": "例如 https://dealnest.example.com，不要带结尾斜杠",
    "default": ""
  },
  "token": {
    "description": "通知 API 令牌",
    "type": "string",
    "hint": "必须与 DealNest 的 BOT_NOTIFICATION_TOKEN 一致",
    "default": ""
  },
  "dealnest_proxy_url": {
    "description": "访问 DealNest 时使用的代理地址",
    "type": "string",
    "hint": "留空直连；支持 http://127.0.0.1:7890、s5://127.0.0.1:1080、socks5://user:pass@host:1080",
    "default": ""
  },
  "admin_qqs": {
    "description": "允许执行插件管理命令的 QQ 号",
    "type": "string",
    "hint": "每行或逗号分隔一个 QQ；AstrBot 全局管理员也会被允许",
    "default": ""
  },
  "target_umos": {
    "description": "AstrBot 群会话 unified_msg_origin 列表",
    "type": "string",
    "hint": "每行一个；推荐在每个目标 QQ 群内发送 /dn_bind_group 自动写入",
    "default": ""
  },
  "batch_size": {
    "description": "单次最多拉取通知数",
    "type": "int",
    "default": 10
  },
  "poll_interval_seconds": {
    "description": "无通知时轮询间隔秒数",
    "type": "int",
    "default": 30
  },
  "failure_backoff_seconds": {
    "description": "DealNest 不可达或请求失败后的退避秒数",
    "type": "int",
    "default": 60
  },
  "request_timeout_seconds": {
    "description": "访问 DealNest 的请求超时秒数",
    "type": "int",
    "default": 15
  }
}
